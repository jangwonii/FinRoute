# FinRoute Web

Next.js advisor workflow shell for FinRoute.

## Local Commands

PowerShell may block `npm.ps1`; use `npm.cmd`.

```powershell
cd apps/web
npm.cmd install
npm.cmd run dev
```

Build:

```powershell
npm.cmd run build
```
